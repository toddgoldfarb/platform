mutation postIntention($eventId: ID!, $text: String!) {
  postIntention(input: { eventId: $eventId, text: $text }) {
    intention {
      _id
      text
      eventId
      userId
      userName
      userAvatar
      userLocation
      ampCount
      createdAt
      userAmplified
      pinned
    }
  }
}
