subscription onIntentionAdded($eventId: ID!) {
  intentionAdded(eventId: $eventId) {
    _id
    text
    userId
    userName
    userAvatar
    userLocation
    ampCount
    createdAt
    userAmplified
    pinned
  }
}
